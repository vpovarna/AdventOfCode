switch (currentOrientation) {
                    case 'E' -> {
                        x = Math.abs(x);
                        y = Math.abs(y);
                    }
                    case 'S' -> {
                        x = Math.abs(x);
                        y = -Math.abs(y);
                    }
                    case 'W' -> {
                        x = -Math.abs(x);
                        y = -Math.abs(y);
                    }
                    case 'N' -> {
                        x = -Math.abs(x);
                        y = Math.abs(y);
                    }
                }